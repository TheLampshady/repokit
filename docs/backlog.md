# Design Backlog

Future improvements and design considerations for repokit.

## Open Items

- [ ] #1 Security scanning — Decide whether dependency vulnerability scanning (pip-audit, npm audit) belongs in repokit at all now that the plugin is context-focused; likely out of scope
- [ ] #2 `.cursor/rules/*.mdc` generation — Auto-generate Cursor rule files from SKILL.md descriptions
- [ ] #3 Skills.sh ecosystem — Monitor for standardization opportunities and consider contributing repokit's format
