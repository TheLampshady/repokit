{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "open=$(grep -c '\\- \\[ \\]' spec/backlog.md 2>/dev/null || echo 0); [ \"$open\" -gt 0 ] && echo \"📋 $open open items in spec/backlog.md\" || true"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Session complete. If code was modified, run sanity-checker to verify quality.'"
          }
        ]
      }
    ]
  }
}
